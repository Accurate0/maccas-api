query Index($includePoints: Boolean!, $minimumCurrentPoints: Int!) {
	offers {
		shortName
		imageBasename
		name
		count
		id
	}
	points(filter: { minimumCurrentPoints: $minimumCurrentPoints }) @include(if: $includePoints) {
		currentPoints
		accountId
	}
}
